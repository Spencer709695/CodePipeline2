version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies......"
  pre_build:
    commands:
      - echo "Running pre-build steps..."
  build:
    commands:
      - echo "Building the app..."
artifacts:
  files:
    - '**/*'
  discard-paths: no